apiVersion: v1
kind: Service
metadata:
  name: prometheus
spec:
  type: ClusterIP            # Internal access only
  selector:
    app: prometheus

  ports:
    - port: 9090             # Service port
      targetPort: 9090       # Container port