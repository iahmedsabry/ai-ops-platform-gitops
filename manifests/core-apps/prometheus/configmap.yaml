apiVersion: v1
kind: ConfigMap
metadata:
  name: prometheus-config   # Must match Deployment reference
data:
  prometheus.yml: |
    global:
      scrape_interval: 15s   # How often to collect metrics

    scrape_configs:
      - job_name: "prometheus"
        static_configs:
          - targets: ["localhost:9090"]   # Prometheus monitors itself